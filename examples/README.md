# Sample output

Two real audits, generated by this agent — open the PDFs to see exactly what a client receives.

| File | What it is |
|---|---|
| `live-demo-report.pdf` / `.html` | A **live public website** (pypi.org), 8 pages crawled. Shows what a healthy, large site looks like: grade D, authority 70, 109 findings, real Core Web Vitals from headless Chrome. |
| `live-demo-result.json` | The full JSON for that audit — every field the API returns. |
| `sample-report.pdf` / `.html` | The bundled test site, which has 24 deliberate faults planted in it. Shows the agent catching every category of issue, with red-highlighted screenshot evidence. |
| `sample-result.json` | The full JSON for that audit. |

Generate your own in two minutes with `./demo.sh` from the project root.
